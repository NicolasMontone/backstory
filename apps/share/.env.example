# Vercel Blob read/write token.
# On Vercel: create a Blob store and link it to this project — the token is
# injected automatically, so you don't need to set anything.
# For local dev: `vercel env pull .env.local` (or paste the token below).
BLOB_READ_WRITE_TOKEN=
